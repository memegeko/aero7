# Release Evidence

Store short, non-confidential release review records here. Each record should
identify the date, reviewer, scope, result, and the private location of any
confidential source document. Never commit contracts, personal information,
credentials, private keys, or access tokens.

The public-release gate remains blocked until `config/public-release.conf`
links an approval record and every required review is marked `cleared`.
